# Release signing configuration for Play Store
# Copy this file to keystore.properties and fill in your values
# DO NOT commit keystore.properties to version control!

# Path to your keystore file (relative to project root or absolute)
storeFile=release.keystore

# Keystore password
storePassword=your_keystore_password

# Key alias within the keystore
keyAlias=carrel

# Key password
keyPassword=your_key_password

# To generate a new keystore, run:
# keytool -genkey -v -keystore release.keystore -alias carrel -keyalg RSA -keysize 2048 -validity 10000
